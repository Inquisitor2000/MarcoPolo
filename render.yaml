# Deploy to Render: https://render.com/deploy?repo=<your-github-url>
services:
  - type: web
    name: marcopolo-relay
    env: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 3000
